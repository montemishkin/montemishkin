/**
 * Style sheet for ProjectSearchView component.
 */

/* local imports */
import numerics from '../../../styles/numerics'


// define style sheet
let styles = {
    container: {
        width: (100 - (2 * numerics.root_child_side_padding.slice(0, -1))) + '%',
        margin: 'auto',
    },
}


// export style sheet
export default styles


// end of file
